questions:
  - variable: custom.image.repository
    description: "URL of container image repository holding containerized app. Defaults to 'redaptcloud/favorite-beer'."
    required: true
    type: string
    default: redaptcloud/favorite-beer
    label: Image Repository
    group: "Favorite beer settings"

  - variable: custom.image.tag
    description: "Container registry image tag. Defaults to 'v1'."
    required: true
    type: string
    default: "v1"
    label: Image Tag
    group: "Favorite beer settings"
